# Normalize line endings to LF in the repo. Critical for the Linux VM deploy:
# a CRLF shell script fails with "\r: command not found".
* text=auto eol=lf

# Binary assets (add as needed) — never normalize.
*.png binary
*.jpg binary
*.gif binary
